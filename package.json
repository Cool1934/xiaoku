{
  "name": "zego-voice-chat-cloudflare",
  "version": "1.0.0",
  "description": "即构 ZEGO 语聊房 Web 端（15 麦位 + 房间信息），部署到 Cloudflare Pages（前端）+ Worker（签 Token04）",
  "scripts": {
    "deploy:worker": "wrangler deploy"
  },
  "devDependencies": {
    "@zegocloud/server-assistant": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}
