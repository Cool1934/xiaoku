name = "zego-token"
compatibility_date = "2024-06-01"
main = "worker/token.js"

[build]
command = "npm install @zegocloud/server-assistant"

# 推荐：把 AppID / Secret 配置为 Worker 环境变量（wrangler secret put APP_ID ...）
# 下面仅为本地开发占位，生产请勿把 secret 明文写在此文件
[vars]
APP_ID = "0"
SERVER_SECRET = ""
